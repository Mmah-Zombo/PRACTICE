#include <stdio.h>
#include 
/**
 * main - makes use of a couple of functions
 * Return - 0 always
*/
int main(void)
{
    int num = 10;
    int num2 = 20;
    int nums[3] = {num, num2};

    for (int i = 0; i < len(nums); i++)
    {
        printf("%d\n", i);
    }
    printf("%d\n", num);
    return (0);
}